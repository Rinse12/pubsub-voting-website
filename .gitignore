node_modules/
dist/
dist-coldtest/
.bitsocial-pubsub-voting/
.pkc/
*.log
tes

# Local Netlify folder
.netlify

# seed-vote wallet key (scripts/seed-candidate-votes.ts) — never commit
.vote-seed-wallet.json

# benchmark raw per-round timelines (large); results/*.md is the committed summary
results/*.jsonl
